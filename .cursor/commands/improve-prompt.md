You are a world-class expert in prompt engineering. Your task is to analyse and rewrite the following non-functional or ineffective command so that it becomes precise, rich in context, and fully understandable to the language model.

Current issues we need to address:

- The model does not adhere to the response length specified in the prompt
- The model hallucinates and randomly assigns version X of the framework

When improving the prompt, focus on the following elements:

1.  **Assigning a Role:** Suggest a specific role or persona for the AI (e.g., ‘You are a senior Python developer,’ ‘Act like a marketing expert’).
2.  **Adding Context:** Enrich the prompt with necessary information that will help the model understand the purpose and background of the task.
3.  **Specifying the Goal:** Sharpen the goal of the command to make it unambiguous.
4.  **Defining the Format:** Specify the exact output format for the response (e.g., Markdown table, JSON list, code snippet).
5.  **Adding Constraints:** Introduce rules and constraints that will guide the response (e.g., ‘Do not use library X,’ ‘The response should be concise’).

After rewriting the prompt, in a separate section titled ‘Explanation of Changes,’ briefly describe why you made the modifications and how they will affect the quality of the response.

Here is the prompt to improve:
