---
import Layout from '../../layouts/Layout.astro';
import { LoginForm } from '../../components/auth/LoginForm';

export const prerender = false;

// Note: Server-side auth validation will be added in next steps
---

<Layout title="Login - 10x Cards">
  <div class="container max-w-md mx-auto mt-8 px-4">
    <div class="text-center mb-8">
      <h1 class="text-4xl font-bold tracking-tight">Welcome Back</h1>
      <p class="text-muted-foreground mt-2">Sign in to your account to continue</p>
    </div>
    <LoginForm client:load />
  </div>
</Layout>
